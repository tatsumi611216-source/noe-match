# noe-match 運用知見台帳

**このファイルの目的**: 実測で確かめた知見を1か所に貯める。日次戦略タスクは
毎回これを読んでから一手を決め、新しい知見が出たら追記する（日付・根拠つき）。
推測は書かない。実測・実施した結果だけを書く。

書式: `- [YYYY-MM-DD] 知見。**根拠:** 何をどう測ったか。`

---

## 勝ち筋（何が効くか）

- [2026-08-24] ツールの勝ち筋は「需要のある語」ではなく**SERP1ページ目に器具（計算機・診断）がまだ無い語**。金銭語（費用・料金・折半）は事業者か計算専業で必ず埋まっている。空くのは「送客に繋がらない」×「更新が面倒」な語（例: ガルガル期診断、入籍日カレンダー）。**根拠:** 同日公開ツール5本の表示格差（garugaru 42表示 vs kekkon-shikin 0表示）＋DDG/GoogleのSERP実査。
- [2026-08-25] 制度の施行日は検索需要の立ち上がりを予告する。大手は施行後にしか書かないので、**施行前に一次データの器具を置けば先回りできる**。こども誰でも通園制度で実証（公開当日にインデックス、2日で5セッション）。**根拠:** 通園ナビの初動＋SERPに競合器具ゼロの実査。
- [2026-08-26] 流入経路ごとに着地するページの型が違う。**Google/Yahoo→ツール、AI（chatgpt/copilot）/Bing→データ記事**（公表値の表＋出典＋確認日を持つページ）。AIは器具を操作できないので体験型には送客しない。**根拠:** GA4ランディング×参照元28日実測。
- [2026-08-26] 滞在2分超で読まれる記事は「結婚率の公表データ」（success-rate-data 735秒/6sess）と「産後の悩み」（garugaru系 250-760秒）の2系統だけ。**根拠:** GA4 userEngagementDuration実測。

- [2026-08-26] **tool-cluster戦略の中間評価: 集客装置としては正解と実証、収益装置としては未実証**。①核は「ツールという形式」ではなく「器具の空白語×需要実測」（8/13の5本中4本は語の選定ミスで死んだ）。②4本足に「⑤データ記事（公表値の表＋出典＝AI・Bingの入口）」を足す。AI経由はツールに来ない。③収益の脚はどのクラスタでも未達（収益0円・LINE友だち1人）。**根拠:** GA4入口実測（ツールが上位独占）＋同日公開5本の格差＋ランディング×参照元クロス。

- [2026-08-26] **選定ミスの構造対策として `scripts/tool_gate.py` を新設**。ツール新設前に必ず通す機械ゲート（需要=サジェスト × 器具の空白=SERP上位の器具語・専業ドメイン検出 → GO/NO-GO/CHECK）。検証: 負け語「結婚式 費用 シミュレーション」をNO-GO（器具7件検出）、際どい語をCHECKに正しく判定。**CHECKとGOでも公開前にGoogleを1回目視する**（判定はDDG=Bing系のため）。専業ドメインを新たに見つけたら INSTRUMENT_DOMAINS に追記。

- [2026-08-27] **tool_gateが実際に本命を1本掘り当てた**。「産後ケア 料金」＝サジェスト10件・SERP1ページ目に器具ゼロでGO。金銭語なのに計算専業も事業者も入っていない空白で、しかも自治体ごとに実額が違う制度型＝通園ナビと同じ型が再現できる。逆に「保育料 いくら 計算」は器具6件でNO-GO、「保活 スケジュール 早見表」はサジェスト0件でNO-GO。**根拠:** tool_gate.py実測＋Bing SERP目視。
- [2026-08-27] **新設ツールは「既に人が着地しているクラスタ」から内部リンクを引く**。GA4実測で最大の入口は /tools/garugaru-check/（14日40セッション）で、産後クラスタが実流入の中心。新器具（産後ケア料金ナビ）へ産後クラスタ14本からアンカーを引いた。順位ゼロの新規ページを単独で置いても入口にならない。**根拠:** GA4ランディング14日実測。

- [2026-08-27] **ゲート通過語は2本とも「解説記事はあるが器具が無い」型だった**。「産後ケア 料金」も「育休 延長 条件」も、SERP上位は官公庁＋解説記事で、計算機・診断・判定ツールがゼロ。**大手が書くのは解説であって器具ではない**。制度・法令まわりは解説が飽和していても器具の席が空いている。**根拠:** DDG＋Bingの1ページ目実査（8/27）。
- [2026-08-27] **法令系の器具は「2系統に分かれている論点」が狙い目**。育休延長は「休業の延長（育児・介護休業法・申出先は勤務先）」と「給付金の延長（雇用保険法・申請先はハローワーク）」が別判定で、2025年4月に厳格化されたのは後者だけ。解説記事はここを混ぜて書いているものが多く、分けて判定するだけで差がつく。**根拠:** 厚労省リーフレット・e-Gov条文の一次確認。

- [2026-08-27] **1本あたりの実力は「データ記事＝器具 ＞ 通常記事の5倍」**（30日・direct除く）。通常記事218本→0.24セッション/本、データ記事25本→1.28、ツール17本→1.41。**器具とデータ記事はほぼ互角**で、通常記事だけが桁違いに低い。記事243本のうち直近30日に1セッション以上あったのは57本（23%）だけ。**根拠:** GA4ランディング×参照元30日実測（記事・ツールを本数で割った）。
- [2026-08-27] **directを含めるとツールの実力を過大評価する**。garugaru-checkはdirect込みだと30日で最大の入口に見えるが、direct除くと6セッション。内部トラフィック除外は8/26からなので、30日窓のdirectには自己流入が残っている。**ページ間の比較は当面directを除いて行う**。**根拠:** 同一クエリのdirect有無での比較。

- [2026-08-27] **投資の単位はページではなくデータセット**。一次データを1回取れば、器具（Google/Yahoo向け）とデータ記事（AI/Bing向け）の2つの面を出せる。この2つは重複ではなく取る経路が違う。産後ケア43自治体の1データセットからツール1本＋記事2本が出て、記事側の追加コストはほぼゼロだった。**堀はページではなくデータセット**（ページは真似できるが43自治体の一次確認は真似しにくい）。台帳は `agent/data_bank.md`。**根拠:** 8/27の実作業と1本あたりの実測（ツール1.41/データ記事1.28/通常記事0.24）。
- [2026-08-27] **データバンクは鮮度管理をしないと負債になる**。「確認日」を明記して信頼を取りに行く型なので、元データが古いまま置くとその確認日が逆に嘘になる。更新期限は資産ごとに違う（生涯未婚率は2026-09-29の国勢調査基本集計、人口動態の確定数は毎年9月・諸率は12月に再計算、育休給付の上限額は毎年8/1、自治体の料金は年度4月）。期限は revenue_pdca.py の DEADLINES に載せて週次で自動監視する。**根拠:** 一次調査で各統計の公表サイクルを確認。

- [2026-08-27] **バンクの「まだ出せる面」は、台帳を書いた直後から古くなる**。データバンク台帳では資産#4（出産費用）が「出している面: まだ無し」のままだったが、実際には同日中に記事2本が公開済みだった。**台帳の更新は面を出したコミットと同じ回で行う**（別作業にすると、翌日の判断が古い台帳で下される）。**根拠:** 8/27の日次戦略で、既に埋まっていた枠を再び最優先候補として拾いかけた。
- [2026-08-27] **同じ正本から2本目・3本目の面を出すのが、いま最も安い一手**。育児休業給付金の記事は、8/27に取得済みの `ikukyu_rules.json` から追加取得ゼロで作れた。表は3枚とも正本から計算しており、手で書いた数字は無い。**データ取得を伴う新規テーマより先に、既存バンクの未使用の面を全部出し切る**。**根拠:** 資産#5の kyufu_rate ブロックだけで表3枚（賃金月額別の実効額・改定前後・給付の種類の横断）が組めた。
- [2026-08-27] **正本から数値を取り出すときは、正本どうしの関係式で検算してから記事を出す**。育休給付金は「支給上限額＝休業開始時賃金月額の上限×支給率」が成り立つので、パースした4つの数値をこの式で assert してから生成する（496,200×0.67＝332,454／×0.50＝248,100で一致）。文字列から数字を拾う処理は8/27に産後ケアの上限判定で誤読の実績があるため、拾ったあとに必ず検算を挟む。**根拠:** build_ikukyu_kyufu_article.py に実装・実行で一致を確認。
- [2026-08-27] **法令データの記事は「よく言われている数字が、いつ・なぜ変わるか」を軸にすると差が出る**。育休給付金は「賃金の67％」と紹介されるが、67％なのは通算180日までで、上限額（賃金月額496,200円）を超えると実効率はさらに下がる。延長期間が50％なのは延長したからではなく日数が180日を超えているからで、解説記事はここを混ぜている。**根拠:** 雇用保険法第61条の7第6項の条文確認＋既存ページの本文grep（サイト内で「育児休業給付金」を含むのは判定ツール1本だけだった）。

- [2026-08-27] **GSCの語の面積には天井があり、順位では届かない**。総表示3,447・総クリック41（CTR1.19%）。仮に全ページが1位になってもCTR28%で月965クリック、全部3位なら379。目標に必要な月2,000〜4,000セッションには、**順位ではなく語の面積を10倍以上に広げる**必要がある。これで「順位改善に投資しない」判断が数字で確定した。**根拠:** GSC 7/28〜8/25の全ページ実測＋CTR仮定での上限計算。
- [2026-08-27] **検索語341語のうち50表示以上はゼロ。83.6%が1〜4表示**、最大37表示（看護師 婚活・39.6位）。クリックが出た語は4語（1.2%）だけ。1〜10位の22語ですら合計129表示＝1語あたり5.9表示。**いま拾えている語は全部ロングテールの塵**で、上位表示の語は31位以下に沈んでいる。**根拠:** GSC by_query 実測。
- [2026-08-27] **表示の65%（2,230）が31位以下で、そこからのクリックは9件**。さらに -guide 系42ページが表示の47%（1,633）を占めてクリック13。**表示数を成果指標にすると、実態と正反対の判断をする。**

- [2026-08-29] **データバンク台帳の「出している面」は、実ファイルと突き合わせないと必ずずれる**。8/27に「台帳は面を出したコミットと同じ回で更新する」と書いた翌々日に、産後ケア（台帳3面→実際6面）・出産費用（2面→3面）で再びずれていた。同じ間違いを2回したので、運用ルールではなく**手順**にする: 面を選ぶ前に必ず `ls articles/` と `grep -rl` で実在を確認してから候補を決める。**根拠:** 8/29に「出産費用 無償化」を最優先候補として拾いかけたが、`articles/shussan-mushouka/` が8/27時点で既に存在した。

- [2026-08-29] **「まだ出せる面」に書かれた語が、既存記事の見出しの中に既にあることがある**。資産#3の「都道府県別の離婚率」は未消化とされていたが、`/articles/rikonritsu-data/` の中に `<h2>都道府県別の離婚率ランキング（47都道府県）` として既にあった。別記事にすれば共食いする。**未消化の判定は記事の有無ではなく、既存記事の見出しまで見る**（`grep -o "<h2[^>]*>[^<]*"` で足りる）。**根拠:** 8/29に「離婚率 都道府県」（data_gate GO・一致10）を作りかけて発見。

- [2026-08-29] **同じデータセットの中でも、面によって「並べられるか」の質が違う**。産後ケアの日帰り型は43自治体すべてが実施していて単価を持つのは31自治体、訪問型は42自治体が実施で単価を持つのは35自治体。**「実施しているか」で割れる面と「金額」で割れる面は別の記事になる**。訪問型は「3類型で最も安い（中央値1,000円・日帰り2,400円）のに、9自治体は回数の枠が他類型と共通なので先に消える」という、金額表からは見えない構造が核になった。**根拠:** `_sangocare_data.py` の day_*/visit_* 列の集計（8/29）。

- [2026-08-29] **記事一覧の件数表記は、`arc-sec` の div 単位ではなく h2/h3 の見出し単位で数える**。`class="arc-link"` を div ごと数えて「31と書いてあるが実数51」と判断し、51に直したら factory_audit が構造エラーを出した（audit は `re.split(r'(<h[23] ...>)')` で区切るので、節の途中にh3があるとそこで別グループになる）。**件数表記を直すときは自分で数えず、factory_audit が言う実数に合わせる。** なお当日2本追加したことで、それまで2件ずれていた表記がたまたま一致していた。**根拠:** 8/29に実際に壊して exit 1 で検出・即revert。

- [2026-08-31] **既存バンクに「法令のルール」を1枚足すと、片方だけでは出ない面が作れる**。出産費用バンク（厚労省の実額）に国税庁の医療費控除のルール（一時金を先に引く→さらに10万円を引く）を掛け合わせると、**妊婦合計負担額の平均から一時金50万円を引いて10万円を超えるのは47都道府県中7都県だけ**（東京・神奈川・宮城・千葉・茨城・埼玉・愛知）、全国平均は92,907円で基準に7,093円届かない、という結論が出た。室料差額等を除いた「出産費用」ベースなら東京都の1件のみ。自治体の追加実査はゼロで、足したのは公開されている法令ページ3枚（No.1120/1122/1124）だけ。**新しい自治体データを取るより、既存バンクに法令の一次情報を掛けるほうが安い。** **根拠:** `scripts/build_shussan_iryohi_koujo.py`（費目内訳の合計＝出産費用／全項目の合計＝妊婦合計負担額／47件・合計負担額≧出産費用 の3本を assert で検算）。

- [2026-08-31] **台帳に書いたサジェスト件数を根拠に面を捨ててはいけない**。data_bank.md の資産#4には「医療費控除・確定申告・帝王切開（いずれもサジェスト3件）」とあったが、8/31に data_gate で測り直すと「出産費用 医療費控除」は**一致10のGO**だった。フレーズの作り方（頭の語の付け方）で件数が変わるため、記録した数値は次に選ぶときの根拠にならない。**面を選ぶ直前に必ず測り直す。** **根拠:** 8/31の data_gate 実測と台帳の記載の食い違い。

- [2026-08-31] **監視スクリプトの対象一覧を手書きにすると、新設した資産が静かに監視から漏れる**。`scripts/bank_freshness.py` はバンク名7本を手書きしており、8/30に新設した `_hitorioya_data` が入らないまま「再確認対象: 0本」と報告していた。台帳更新を忘れる事故（8/27・8/29に2回記録済み）と同じ形が、今度はスクリプト側で起きた。**`scripts/_*_data.py` のグロブに変更した。** 台帳やリストに「覚えていないと漏れる」構造を残さない。**根拠:** 8/31に実行し、8本目が出ないことを確認してから修正。

- [2026-08-31] **記事一覧の件数表記が数えているのは `arc-no` が数字の項目だけ**で、「＋」の項目は入らない。＋で1本足したときに件数表記を+1すると factory_audit が構造エラーを出す（8/29に「自分で数えず audit の実数に合わせる」と書いたことの具体形）。データ記事は全部＋で追加しているので、**データ記事を足すときは件数表記を触らない**。**根拠:** 31→32に直して exit 1、31に戻して exit 0。

## 負け筋（何が効かないか）

- [2026-08-26] **記事の量産は限界効用ゼロ**。表示+27%（2,601→3,302）でクリック±0。31位以下の801表示からクリック0件。195本で月39クリック＝1本あたり月0.2クリック。**根拠:** GSC 7期分のKPI推移。
- [2026-08-26] **地域ガイドの表示は幻**。GSC表示800超（tokyo 222/kyoto 196/osaka 122/nagoya 118）に対しGA4実セッション0。40〜50位の表示は実流入を生まない。追加投資しない。**根拠:** GSC×GA4のページ別突合。
- [2026-08-25] 政令市への地域展開で「上乗せの差」は再現しない。時間上乗せは東京23区にほぼ固有（政令市20市で京都の12時間のみ）。地方展開の価値は「差」ではなく「網羅」に切り替わる。**根拠:** 46自治体の一次確認。
- [2026-06〜08] 婚活の収益語（相談所・アプリ比較）は大手占有で3か月やって60位台。この経路の回収は当面成立しない。**根拠:** 相談所クラスタ平均66.5位・0クリック。

## SEOの実務知見

- [2026-08-24] **アンカーテキストはクエリ信号を渡す**。ブランド語アンカー（「〜シミュレーターへ→」47本）は被リンク数が多くても順位に効かない。検索語入りの文言に変える。**根拠:** 被リンク33本で表示0のツール vs 検索語アンカーで9位のツールの対比。
- [2026-08-24] タイトルは実際に表示が出ているクエリの語順に寄せる（「〜はいくら？」→「〜の相場は？」等）。機能名（〜計算機・〜シミュレーター）はサジェスト0〜1件＝誰も検索していない。**根拠:** serp_screen.py実測＋ツール9本改名。
- [2026-08-26] GSC「検出-未登録」「URL未認識」は申請しなくても数日で自力登録されることがある。sitemap＋内部リンクがあれば急がなくてよい。**根拠:** 練馬保活・産後リカバリーが8/24未登録→8/26登録済み。
- [2026-08-26] IndexNowはCIに組み込める（deploy後step・冪等・218URLで200）。Bing系（実流入の4割）の反映を待ち時間ゼロにできる。**根拠:** pages.ymlで実装・CIログ確認。

- [2026-08-27] **サジェスト件数はボリュームの代理指標になっていない**。自社GSCで20位以内にいる20語を検証したところ、サジェスト4件以上の語は平均8.0表示、3件以下の語は平均9.7表示で**相関がなかった**。さらに1ページ目（7〜10位）にいる語ですら表示は2〜35・中央値6。サジェストは「誰かが打っているか」の有無しか示していない。**根拠:** GSC実測20語×サジェスト実測。
- [2026-08-27] **旧ゲートは自社最強の語を落としていた**。「ガルガル期 診断」は完全一致のサジェストが1件でCHECK判定になるが、実際にはGSCで30表示・9.3位・2クリックと自社で最も効いている語。頭の語「ガルガル期」で見れば10件ある。**完全一致フレーズではなく頭の語の豊かさを見る**。data_gate.py に頭の語のサジェストと自社GSC実表示の照会を追加した（2026-08-27）。
- [2026-08-27] **面積 ＝ ボリューム × 1ページ目に入れる確率**。婚活クラスタは355語1,026表示で平均59.1位（1語2.9表示）、産後クラスタは17語110表示で平均38.1位（1語6.5表示）。**ボリュームが小さくても1ページ目に入れる語のほうが面積は大きい。**通園ナビは公開2日で4.7位。**根拠:** GSCクラスタ別実測。
- [2026-08-27] **表記ゆれを増やしても面積は増えない**。30s-konkatsu は19語を拾っているが全部「30代の婚活アプリ」の表記ゆれで、合計45表示・全部60〜99位。対してガルガル期は5語で異なる意図（診断・由来・同居・ない人・名前の由来）を拾い76表示。**増やすべきは語数ではなく独立した検索意図の数。**

- [2026-08-27] **Bing Webmaster Toolsのキーワード調査は使えない**。実ボリュームを取ろうとしたが、語・国・言語を問わず "Invalid request" を返す（日本語でも英語でも同じ）。Search Performance も「48時間待て」の状態でデータ未生成。バナーに「レガシーSOAP/POX APIを2026年8月31日に廃止」の告知が出ている時期と重なる。**絶対ボリュームを取る手段は現時点で無い**（キーワードプランナー等の外部ツールが要る）。**根拠:** 2026-08-27 に実機で確認。
- [2026-08-27] **絶対ボリュームの代わりに「意図の面積」を測る**。`scripts/keyword_expand.py` を新設。頭の語にあ〜んを付けてサジェストを総なめし、独立した問いがいくつぶら下がっているかを数える（ラッコキーワードの無料機能と同じ処理を自前で実行）。「産後ケア」で**430フレーズ・393種の意図**が出た。うち43自治体バンクで答えられるのは36種（9%）。**いま出しているのは3本なので、同じバンクからまだ出せる面が大量にある。**
- [2026-08-27] **地域語を切り捨てたのは早計だった**。「産後ケア 東京 比較」はサジェスト0だが、「産後ケア 東京」「産後ケア 都内」「産後ケア 大田区」「産後ケア 港区」はサジェストに存在する。**フレーズの作り方を間違えると需要があるのに無いと判定する。**頭の語を展開してから絞る手順に変える。

## 計測の実務知見

- [2026-08-26] GA4のdirect流入は運営者の自己流入が混ざる（8/25以前はdirect 51%）。**内部トラフィック除外の有効化は8/26。比較の起点は8/26以降**。A8のクリック数も同様に自己検証クリックが混ざる（7月実測: GSC 3クリックの月にA8 52クリック）。
- [2026-08-26] 実流入の7割が非Google（bing+yahoo 4割・AI 2割強）。**GSCだけ見て意思決定すると流入の3割しか見ていない**。Bing Webmaster＋GA4を必ず併読する。
- [2026-08-25] 正規表現による本文の自動分類は使えない。減免の「生活保護世帯は無料」を無償と誤読、「施設により異なる」を施設ごとと誤読。**分類は人（またはLLM）が読んで確定させる**。
- [2026-08-27] **制度データは「自治体としての単価が存在しない」ケースが一定数ある**。産後ケア23区の実査で、宿泊型の数え方が3通りに割れた（trip=1泊2日＋以降1日ごと／day=1日あたり・1泊2日は2日ぶん／range=施設額−区負担額の差額方式で区としての単価なし）。rangeを1つの数字に丸めると最大3.7倍取り違える（台東区: 3,000〜11,000円/日）。**計算に使う数字と、表示するだけの数字を分ける。** また金額がHTMLに無くPDF・画像の中にしかない自治体が複数あった（新宿・台東・大田は画像、江東・世田谷は条例PDF）。HTMLだけ見ると「非公表」と誤判定する。**根拠:** 23区の一次確認（8体並列）。
- [2026-08-27] **一次情報が自治体の公式ドメインに存在しないことがある**。浜松市は市民向けの産後ケアページ自体が市サイトに無く、制度案内・料金・施設一覧・チラシがすべて外部のNPOサイトにホストされていた。「公式ドメインのみ」を条件にすると原理的に取得不能になる。この場合は**推測で埋めず「一次確認できず」として出す**。金額がHTMLに無くPDFや画像の中にしかない自治体も多数（新宿・台東・大田は画像、江東・世田谷は条例PDF、広島は上限がチラシPDFのみ）。**根拠:** 産後ケア43自治体の実査。
- [2026-08-27] **料金は「単価×回数」で表せないことがある**。相模原市は宿泊6日目から単価が倍、新潟市は各類型の1回目が無料、熊本市・北九州市は2泊目以降の額が非公表。さらに「1泊2日」の数え方が隣接自治体でも逆（名古屋＝2日ぶん／京都＝1回）。**器具のデータ構造は「n回目の単価の並び」にする**と全部を1つのモデルで正しく扱える（最後の値が以降に繰り返す・要素がNoneなら計算しない）。**根拠:** 産後ケアナビの実装と43自治体の実査。
- [2026-08-27] **文字列から数字を拾う自動判定は上限にも使えない**。「1回の出産につき5日まで」の先頭の"1"を上限と読み、上限内の入力を超過と誤判定した。上限は自治体で数え方が違う（類型ごと／3類型合算／宿泊＋日帰りだけ合算）ので、**判定をやめて原文を出す**方が正確。**根拠:** 産後ケアナビの動作検証で検出・修正。
- [2026-08-25] エージェント並列実査は結果をファイルに書かせてから返させる（プロセス死で全損した8体の教訓）。1市=1エージェント×12並列で政令市を1時間で完走。

- [2026-08-27] **インデックス済み＝表示が出る、ではない**。産後リカバリー診断は公開4日で「Submitted and indexed」（8/25クロール・PASS）だが、GSC表示は0。内部リンクは31本あったがアンカーの大半が「産後リカバリー診断｜…」というブランド語先頭で、8/24に実証済みの「効かないパターン」そのものだった。**インデックスの有無と順位は別問題として切り分ける**。**根拠:** URL検査API＋GSC実測＋内部リンクのアンカー集計。
- [2026-08-27] **アンカーの一括置換は、リンクの中身ごと差し替えると構造を壊す**。トップのツールカードは `<a>` の中に `<div class="tool-name">` と `<p>` を持つため、アンカー文字列を置換したらカードが1行に潰れた。**ブロック要素を含むリンクは触らないガードを入れる**（scripts/fix_sango_recovery_anchors.py に実装）。**根拠:** 実際に壊して git diff で検出・復旧した。

- [2026-08-27] **データ記事の量産の制約は制作能力ではなく「需要のある語の在庫」**。自分で考えたひねった語（産後ケア 東京 比較／こども誰でも通園制度 自治体 一覧）はサジェスト0件、素直な語（◯◯ 平均・相場・率）は27語中24語がGO。既存ページと突き合わせると作れるのは14語だった。**在庫が尽きたら止める**。無理に語を増やすと通常記事の0.24に戻る。ゲートは `scripts/data_gate.py`、台帳は `agent/data_candidates.md`。**根拠:** サジェスト実測27語＋既存ページとの突合。
- [2026-08-27] **デプロイ完了の確認は「ワークフロー名＋headSha」で見る**。`gh run list --limit 1` の先頭は別ワークフロー（Factory Audit）のことがあり、それを完了と誤読して本番404のまま「反映済み」と判断しかけた。`gh run list --workflow "Deploy to Pages"` で headSha を突き合わせる。**根拠:** 8/27に実際に誤判定した。

## 運用の落とし穴

- [2026-08-26] GSCのURL検査はディープリンク不可（404）。上部検索バー経由のみ。モーダル表示中のEnterは「公開URLテスト」が走る。ボタンはfindでref取得してクリック。
- [2026-08-27] **pushしてもDeploy to Pagesが発火しないことがある**。tools/・articles/・sitemap.xml を含むコミット（paths-ignore の対象外）をpushしたのにワークフローの実行が作られず、本番は404のままだった。`gh workflow run "Deploy to Pages" --ref main` の手動ディスパッチで復旧。**公開したら必ず本番URLを叩いて200を確認する**（run一覧のsuccessは前のコミットのものかもしれない。headShaを見る）。**根拠:** 8/27 産後ケアナビ公開時に発生・手動実行で解消。
- [2026-08-25] GitHub Pagesのデプロイは許可リスト方式（pages.ymlで明示コピー）。ルートに置いただけのファイルは公開されない。
- [2026-08-28] **同じリポジトリで別セッションが同時に動いていると、`git add -A` に作業中の変更を吸われる**。GSCアーカイブの workflow と `gsc_archive.py` の修正が、note運用の別セッションのコミット（`77c3037`「note恒常運用のリズムを確定」）に丸ごと入った。内容は無傷でpushもされたが、**コミットメッセージと中身が食い違うので、後から履歴を読んでも何をいつ変えたか分からなくなる**。自分のコミットが「nothing to commit」で返ってきたら、消えたのではなく他セッションに取り込まれたことを先に疑う（`git show --stat HEAD` で確認できる）。**根拠:** 8/28に実際に発生。
- [2026-08-28] **定期実行をローカルPCに置くと、データ取得系は静かに欠測する**。GSCは16ヶ月で消えるので、落とした日は永久に取り返せない。旧 `NOE-GSC-Weekly-Fetch`（Windowsタスク）はPC非稼働で2週不発の前科がある。**取得系はGitHub Actionsに置く**。加えて、取得スクリプトは最初から「直近N日の欠損を埋め直す差分モード」で書いておくと、数日落ちても自力で復旧する（`gsc_archive.py` は45日）。**根拠:** 8/28に `gsc-archive.yml` を新設し、workflow_dispatch でCI経路（secrets.GSC_KEY_JSON）まで実証。
- [2026-08-28] **CIで動かす前提のスクリプトは、認証をファイルパス直書きにしない**。`gsc_archive.py` は `KEY_PATH` 固定だったのでCIに載せられなかった。同じリポジトリの `fetch_gsc.py` は最初から「環境変数→ファイル」の2経路で書いてあり、そのままCIに載っている。**新しい取得スクリプトは `fetch_gsc.py` の認証部をコピーして書き始める。**
- [2026-08-23] sitemap_add.pyはArticle/BlogPostingのJSON-LDしか見ない→WebApplication追加済み（8/25）。新形式のページを作ったらsitemap登録経路を確認する。
- [2026-08-11] 文字数計測はbody_text()（script/style/目次/PR表記除外）で。バイト数計測は誤判定する。

- [2026-08-27] **通園ナビが公開2日でGSCに出た（6表示・1クリック・4.7位）**。制度クラスタで初めて上位表示が確認できた事例。婚活クラスタの平均59.1位と比べると桁が違う。**器具×一次データ×施行前の型は、少なくとも順位を取れる**ことがここで示された（流入量は未確認）。**根拠:** GSC 7/28〜8/25実測。

- [2026-08-31] **自治体の横断バンクは「県の公表一覧」を母集団に取ると全数が確定する。** 結婚新生活支援は市区町村ごとの実施で、検索やまとめサイトからでは母集団が決まらない。千葉県 r8sicyousonzigyou（30市町村）・埼玉県 r8koufukin（17市町）・神奈川県 恋カナ！（13市町村）・東京都 TOKYOふたりSTORY と、県が公表している一覧を起点にすると「何自治体あるか」が言い切れる。**バンクの価値は網羅の証明にあるので、母集団の出所を先に決める。** **根拠:** 8/31に61自治体を全数実査。
- [2026-08-31] **都県の公式ポータルのリンクは死んでいることがある。** 東京都「TOKYOふたりSTORY」から貼られている青梅市の結婚新生活支援ページは404で、市の現行制度は住居費補助ではなく「お祝い金2.2万円＋5年後の応援金」に変わっていた。**上位機関の一覧は母集団の確定には使えるが、内容の確認には使えない。必ず自治体ページまで降りる。** **根拠:** 8/31実測（curl 404・市サイトの制度一覧にも旧制度なし）。
- [2026-08-31] **「一都三県で最も低い／最長」のような全称は、データ側に書くと記事にそのまま出る。** 川口市を「一都三県で最も低い上限額」と書いたが実際は寒川町6万円が下、山北町の居住10年を「一都三県で最長」と書いたが居住年数は全自治体で採取していなかった。**データモジュールの note 欄に最上級を書かない。**最上級は記事生成時に全数から機械的に出す。**根拠:** 8/31の公開前検品でBLOCK（5件中2件がこの型）。

- [2026-09-01] **結婚新生活支援の検索需要を実測した（器具を出したあとに測った＝順序を間違えた）。** aramakijake（Google推定・月間）: 結婚 助成金 6,480／結婚 補助金 1,520／結婚新生活支援事業 1,280／新婚 補助金 384／結婚 補助金 60万 40／新婚 引っ越し 補助金 8。自治体名つき（結婚新生活支援事業＋東京・千葉県・埼玉）と「条件」「いくら」系は**未収録＝計測不能（数十以下）**。比較: ガルガル期6,480・夫源病7,920・蛙化現象48,400。
  **読み: 頭語はガルガル期と同水準あるが、その頭語（結婚 助成金・結婚 補助金）は全国語で、当サイトは一都三県61自治体しか持っていない。** 制度の正式名（1,280）は中規模、ロングテールは1語あたり数十以下で、61自治体を積んでも数百にしかならない。**「全数を持っている」ことが効くのは横断比較の意図だけで、量そのものは大きくない。**
  自社GSC実測（7/31〜8/28）でも、補助・助成・新生活・給付金・支援事業を含むクエリは**409クエリ中0件**。既存記事 `shinkon-hojokin` は表示3・クリック0・平均55.3位。**この語群では現時点で1件も表示が取れていない。**
- [2026-09-01] **器具のゲートは「サジェストの本数」だけで通してはいけない。月間検索数まで測る。** 8/31のゲートは tool_gate.py（サジェスト＝需要／SERPの器具有無＝空白）で条件付きGOにしたが、tool_gate は**検索数を見ていない**。通園ナビ・産後ケアは事前に検索量を実測していたのに、結婚新生活支援は飛ばした。**ゲートの2軸（需要・器具の空白）に「月間検索数」を足す。** 具体的には aramakijake の `https://aramakijake.jp/keyword/index.php?keyword=<URLエンコード>` をGETし、「月間推定検索数」直後の2つの数値（1つ目Yahoo・2つ目Google／比は常に1:4）を読む。POSTは通らない。**根拠:** 9/1の実測と、8/31にこの手順を踏まなかったこと。

- [2026-09-01] **GSCのURL検査を自動操作するときは、検索窓に文字が入ったことを確認してから送信する。** 検索窓へのクリックはCDP経由だと**フォーカスが入らないことが半分くらいある**。入っていないまま type→Enter すると、**直前に検査したURLのページが再表示されるだけ**で、そこで「インデックス登録をリクエスト」を押すと**同じURLを二重申請する**（9/1に2件やった）。手順は「①左メニューの『URL 検査』をクリック（単独の呼び出しにする。バッチの中に入れるとフォーカスが入らない）→ ②履歴のドロップダウンが開いたことをスクリーンショットで確認 → ③URLを入力してEnter → ④**ページ上部のURL表記を拡大して、意図したURLに変わったことを確認** → ⑤ボタンを押す」。**④を飛ばさない。** また、ボタンは座標で押さない（レイアウトが状態で変わり、9/1は「公開URLをテスト」を誤爆した）。**根拠:** 9/1の申請作業で二重申請2件・誤爆1件。
- [2026-09-01] **申請キューは台帳の記憶ではなく index_status.json と突き合わせて作る。** 台帳の「残り21本」を実測（URL Inspection API・251URL）と突き合わせたら、未インデックスかつ未申請は**13本**だった。残りは既に入っていた。**申請枠は1日10本しかないので、既に入っているURLに枠を使うと純粋な損失になる。** **根拠:** 9/1の突き合わせ。

- [2026-09-01] **器具ゲートのしきい値を実測で較正した（月間検索数・Google推定）。** 既存ツールの語で測ると、成功群と失敗群がきれいに割れた。成功＝ガルガル期6,480／保育園 点数1,920／子ども医療費 助成1,040／こども誰でも通園制度208。失敗＝婚活 タイプ 診断8／再現婚・結婚資金 計算は**aramakijake未収録**。**成功群の下限は208、失敗群は8以下か未収録。** よって `tool_gate.py` に第3軸として検索数を実装し、**200以上=GO / 100〜199=CHECK / 100未満・未収録=NO-GO** とした。**「サジェストが何件あるか」は枝分かれの数であって人数ではない。** **根拠:** 9/1に8語を実測してtool_gateに実装、婚活タイプ診断がNO-GOで再現することを確認。

- [2026-09-01] **GA4の日次取得を自動化した。鍵は既にあった。** GSCだけ自動でGA4は手動読みという片肺状態を解消するため `scripts/fetch_ga4.py` と `.github/workflows/ga4-archive.yml` を新設。**設定を足す前に既存の権限を見ること**——GA4のプロパティのアクセス管理には既に `gsc-ga4-analytics@matching-app-analytics.iam.gserviceaccount.com` が閲覧者として入っており、その鍵も `Downloads/matching-app-analytics-*.json` に落ちていた。**新しいサービスアカウントに権限を付ける必要はなかった**（noe-gsc側でAPIを2つ有効化したのは結果的に不要）。プロパティは `properties/549779769`、鍵は `secrets/noe-ga4-key.json` に配置。GSC用の `noe-gsc-reader` とは**別プロジェクト・別アカウント**なので、CIのシークレットも `GA4_KEY_JSON` と分ける。
- [2026-09-01] **GA4を見ると、AI経由が全流入の1〜2割ある。GSCだけでは見えない。** 直近28日（Direct除き215セッション）で chatgpt.com 47・copilot.com 15＝**62セッション**。**データ記事（統計）は47セッション中23（49%）がAI経由**なのに対し、ツールはAI経由が13%しかない。**AIは器具を操作できないので記事に来て、Googleは器具を上に出す**という仮説と一致する。**Directが全体の56%（272）を占めるが、これは自社の検品作業が混じっているとみて除いて読む**（8/26に内部トラフィック除外を入れたがエージェントのブラウザは対象外）。

- [2026-09-01→**9/4に結論を訂正**] **A8のクリック数とGA4の aff_click が25倍食い違っている。** A8サイト別レポート（noe-match単体）は**8月単月49クリック・6〜9月累計98クリック・発生0**。同じ期間のGA4 `aff_click` は**45日でわずか2件**（8/27に2件のみ）。**A8はpx.a8.netへのリダイレクトを直接数えるので、A8側が実態に近い。GA4のイベントが落ちている。** 原因は未特定（新規タブ遷移・トラッキング防止・リスナーの取りこぼしのいずれか）。
  **影響: 「どのページの広告が押されたか」をGA4で測る計画は、このままでは機能しない。** A8にはページ単位の内訳がないため、**ページ別の広告効果を測る手段が今は無い**。対策の候補は(1)A8リンクのURLパラメータ機能でページを識別する (2)GA4側の送信をbeacon化しauxclick/pointerdownも拾う。**根拠:** 9/1にA8管理画面（media-console）とGA4アーカイブを同一期間で突き合わせ。
- [2026-09-04] **【訂正】食い違いの原因はGA4ではなくA8。A8のクリック数は実需の指標として使えない。** 9/1は「A8が実態に近く、GA4のイベントが落ちている」と結論したが逆だった。**GA4には自作リスナーとは別の、内蔵の outbound click 計測（`click` イベント）が最初から動いていた。** **GA4にデータがある8/13〜9/3の22日**で `linkDomain=px.a8.net` の click は**6件**（同期間のA8は概算30件）（自作 `aff_click` は2件）。自作リスナーの取りこぼしは6→2の差だけで、**25倍を説明できるのはA8側**。決定打は7月の実測で、**GSCクリック3件しかない月にA8は52クリックを計上している**——A8の数字は実トラフィックとほぼ無相関に動く。JS計測を通らずpx.a8.netへのGETだけが発生する経路（クローラのhref追跡・プリフェッチ・リンクプレビュー・A8側の巡回、および**GA4の内部トラフィック除外が効く自社検品はA8では除外されない**）が積み上がっているとみる。**実クリックは月4件程度。** **根拠:** GA4 Data APIで eventName=click × linkDomain × linkUrl を実測（9/4）。
- [2026-09-04] **ページ別の広告効果は、内蔵の click イベントで既に測れる。9/1に「測る手段が無い」と書いたのは誤り。** `eventName=click` に `pagePath` と `linkUrl` のディメンションが乗るので、**どのページのどの案件が押されたかまで取れる**。8/13〜9/3の実績は /tools/garugaru-check/ 3・/articles/bachelor-date-guide/ 1・/articles/dousei-hajimekata/ 1・/articles/nurse-guide/ 1。**自作の `aff_click` は内蔵より情報が少なく件数も取りこぼすので、収益導線の正式な計器は内蔵 click に一本化する**（自作リスナーの改修は不要）。**根拠:** GA4 Data API実測（9/4）。
- [2026-09-04] **A8の「クリック250〜300で転換率を判定する」計画は成立しない。** 実クリックが月4件なら250件に5年かかる。11月のアフィリ判定は、A8の水増しされたクリック数を前提にしていた。**判定の分母はGA4の click に置き換える必要がある。** **根拠:** 上2件。
- [2026-09-04] **GA4のセッション数を29%水増ししていた。** `fetch_ga4.py` が日次の合計を by_page（pagePath×channel×source）の行の足し上げで作っており、**複数ページを見たセッションが行の数だけ重複していた**。8月は487と出ていたが実数は363。「月570セッション」「242面で1面あたり月2.0セッション」といった判断は、この水増し値の上に載っていた。**合計は必ずディメンションなしで取り直す。** 修正済み・既存アーカイブ45日ぶんも実数に置き換え（旧値は `total_by_page_sum` に残した）。**根拠:** sessions指標の直接取得と突き合わせ（9/4）。
- [2026-09-04] **GA4のデータは2026-08-13より前が存在しない。** 5/1まで遡って照会しても行が返らない。アーカイブの7/17〜8/12の27日ぶんは中身が空のまま並んでいるだけで、**「45日」と書いてきた窓は実質22日**。GA4基準で語れるのは8/13以降だけで、**それ以前の比較対象はGSCとA8しかない**。**根拠:** GA4 Data APIに2026-05-01〜08-14で照会（9/4）。
- [2026-09-04] **9/3のガルガルCTA配置判定は「判定不能」。3つ同時に壊れていた。** ①判定材料の `line_add_click` の `pos` は**GA4にカスタムディメンションが1つも登録されていない**ためAPIで切り分けられない（GA4は登録した`event_parameter`しかレポートに出せない）。②発火した3件は**すべて実装当日の8/27**で、同日にaff_click 2件も出ている——自社検品の可能性が高い。③その後 garugaru-check のセッションが崩れ、母数が消えた（前14日 3.64/日 → 後8日 0.62/日、**8/31以降はゼロ**）。**「1件でも出れば勝ち」という基準は、自己流入と区別できない時点で基準になっていなかった。** 対策として**位置をイベント名に持たせる**（`line_add_result` / `line_add_bottom`）。登録なしで測れる。**根拠:** GA4 Data APIのメタデータ照会とイベント実測（9/4）。
- [2026-09-04] **イベントのパラメータは、GA4で「カスタムディメンション」に登録するまでレポートに出ない。** 当社は登録がゼロなので、`pos` `tool` `from` `to` `link_domain` などこれまで送ってきたパラメータは**すべて集計できない状態**だった。内蔵イベント（`click` の `linkUrl`・`pagePath` など）は登録不要で出る。**設計の原則: 切り分けたい軸はイベント名に入れるか、内蔵ディメンションで表現する。** パラメータに入れるのは登録を済ませてからにする。**根拠:** GetMetadata で customEvent:* が0件（9/4）。
- [2026-09-04→**同日訂正**] **garugaru-check の「流入消失」は、大半が自社トラフィックの消失だった。** Directを含めて見ると 8/13〜8/22 は日4.7セッションで8/23〜9/2 は日0.8——83%減に見える。しかし**内訳はDirectが38→4**で、**Direct除きの実流入は日0.9→0.5**（10日で9、11日で5）。8月中旬の作り込み期に自分たちが何度も開いていたぶんが乗っていただけで、**ページが死んだのではない**。**Directを含めたページ別の数字は、作業中のページほど大きく出る。ページ単位の判断では必ずDirectを外す。** `cluster_report.py` は最初からDirect除きなので、クラスタAが22→20とほぼ横ばいに見えていた方が正しい。**根拠:** GA4のチャネル別を前後で比較（9/4）。
- [2026-09-04] **8/27の知見「最大の入口は garugaru-check（14日40セッション）」は、Direct込みの数字だった。** Direct除きでは同期間9セッション程度。**新設ツールへ内部リンクを引く判断そのものは変わらない**が、根拠にした数字は4倍に膨らんでいた。
- [2026-09-04] **クラスタごとに流入エンジンが違う。同じ施策を全クラスタに当てても効かない。** 21日のDirect除き実測: **D（アプリ選定）はChatGPT依存**（51セッション中33＝65%・members-data は8件すべてchatgpt）、**E（アプリ実績データ）はBing＋Copilot**、**A（ガルガル・産後）はGoogle＋Yahooで AI経由ゼロ**（42セッション）、C（婚活手段）は散在。**AI向けにはデータ記事、検索向けにはツール**という8/26の仮説が、クラスタ単位で裏づけられた形。**Aに効いた施策をDへ持って行っても効かない**（Dの読者はそもそもツールを操作しに来ていない）。**根拠:** `cluster_report.py --days 22` とページ別ドリルダウン（9/4）。
- [2026-09-04] **広告の配分が流入と逆になっている。** 21日のDirect除きセッションと広告本数: C 25セッションに**広告64本**（クリック1）、E 35に42本（0）、B 12に34本（1）、C-sub **2セッションに17本**（0）、J 28に23本（0）。一方 **A は42セッションに35本でクリック3**——**6件中3件がAだけで出ている**。広告291本を22日回して6クリック。**本数ではなく、押される場所（結果の直後・ツールの周辺）に寄せるべき**という結論は、クラスタ別に見ても変わらない。**根拠:** 同上。
- [2026-09-04] **Dクラスタが「セッション最大・クリック1件」だった理由は3つ。** ①**到達しない**——オファーは本文の55〜92%地点にあり、90%スクロール到達は matching-app-ranking 11%（19PV中2）・members-data 11%（9PV中1）。②**滞在が短い**——Dの3分の2はChatGPT経由で、chatgpt.com は**35秒/セッション・関与率40%**（bingは74秒・72%、yahooは41秒・59%）。**数字を1つ確かめて離脱する読者**が主体。③**案件が1つしかない**——Dの広告は全ページ**ユーブライド（afi-b）1件のみ**で、読者が比べに来ている Pairs・with・タップル・Omiai の案件を当社は1つも持っていない。**①は直せる。②は読者の性質。③は提携を増やすしかない。** **根拠:** GA4のscroll/PV・参照元別の関与・全記事の広告リンク棚卸し（9/4）。
- [2026-09-04] **文書内の文字位置と、実際の画面上の位置はまったく違う。配置の判断は必ず実画面で測る。** members-data で「最初の表の直後」は**文字位置では59%**だが、**実画面では13%**だった（表と長い後半がレンダリングされる比率が違うため）。文字数で見積もって「59%ならまだ深い」と判断しかけたが、実測したら十分に浅かった。**ページは19〜33画面ぶんある。** 測り方: ローカルサーバでiframeに読み込み `getBoundingClientRect().top / scrollHeight`。**根拠:** 9/4の実測（375×812で11ページ）。
- [2026-09-04] **【事故】広告の移設スクリプトが記事の節を3つ巻き込んで切り出した。** 「広告リンクの直前の `<div>` から次の `</div>` まで」をブロックとみなす実装にしたところ、**本文中のインラインリンク**（matching-app-ranking の「真剣婚活に特化したい場合は<a…>ユーブライド</a>」）に当たり、囲んでいた `table-wrap` を起点に h2 3つと表2つを移動した。全23ファイルを `git checkout` で戻して作り直した。**再発防止として2つ入れた**: ①対象を**PR表記を持つ中央寄せdiv**に限定し、本文中リンクのページは触らない ②**移設は長さとタグ数（table/h2/h3/p/div）が不変**でなければ ABORT。**HTMLの構造を正規表現で切り出すときは、掴んだものが本当に自己完結しているかを別の条件で検算する。** **根拠:** 9/4の実機。
- [2026-09-01] **CTAが本文の最下部にあり、到達率11%の帯に置かれている。** GA4の90%スクロール到達は page_view 630に対し70＝**11%**。一方、制度ツールの広告位置は本文の99%（こども誰でも通園ナビ）、LINE CTAは `</article>` の外側（112〜128%）。**器具は tool_calc が押されている（28日で27件）のに、結果の直後には何も置いていない。** tool_cross（器具間の回遊）は9ツールに実装済みで45日**0件**。**根拠:** 9/1のGA4イベントとHTML内の位置測定。

- [2026-09-05] **広告の位置は GA4 の内蔵ディメンション `linkId` で切り分けられる（カスタムディメンション登録不要）。** 9/4の教訓（パラメータは登録しないと出ない）の続き。内蔵 `click` イベントには `linkId` `linkClasses` `linkText` が最初から乗るので、アンカーに `id="aff-result"` を付ければ「結果直後で押されたか」を `eventName=click × linkId × pagePath` で取れる。制度ツール7本のPRブロックを `#result` 内・LINE CTA直後へ移設（DOM実測: 結果表示後のページ高の29%地点、移設前は末尾）。**`#result` は `display:none` で結果表示後にしか出ない**ので、表示回数は「結果を出した人」に限られる。判定時はこの分母で読む。**根拠:** 9/5の移設と検品（PASS）。
- [2026-09-05] **制度ツール5本の Oisix リンクは媒体ID `45C0YR`（台帳は `4B8B4Q`）。** `scripts/_article_shell.py:16` の定数由来で、台帳にも knowledge にも記載がない。成果が別媒体に付いている可能性があるが A8 未ログインで未検証。**A8管理画面で確認するまで、制度ツールの発生0を「転換しない」と読まない。** **根拠:** 9/5の検品で発見。
- [2026-09-05] **移設スクリプトの安全装置は3つで足りた。** ①PRブロックの検出は「PR表記を持つ div」で1ページ1件を assert ②`<div` `</div>` の個数がブロック内で1:1 ③移設前後でタグ種別11個の個数が不変・全長の差が80文字未満。9/4の事故（節3つを巻き込み）はこの3つのどれかで止まった。

- [2026-09-16] **GA4日別アーカイブが12日ぶん欠けていた。CIは全期間 success のまま。** 原因は2つが重なったもの。(1) `GA4_KEY_JSON` が**リポジトリのシークレットに一度も登録されていなかった**（`gh secret list` に GSC_KEY_JSON しか無い）。9/1に「CIを新設した」と書いたが、登録したのはワークフローだけで鍵は入れていない。`fetch_ga4.py` はシークレットが無いとローカルの絶対パス（`C:\Users\tatsu\matching-app\secrets\noe-ga4-key.json`）に落ちるので、CIでは毎回 FileNotFoundError で認証に失敗していた。(2) **`python ... | tee` はパイプの終了ステータスが tee のものになる**ので、スクリプトが 1 を返しても step は成功扱いになる。(1)だけなら赤くなって気づけた。**根拠:** `gh run view 35035710045 --log` に「認証・プロパティ解決に失敗: FileNotFoundError」が出ているのに run は success、かつ agent/ga4_archive が 2026-09-02 で止まっていた。
- [2026-09-16] **緑のCIは「動いた」証拠にならない。出力物の最新日付で死活を見る。** 対策として ga4-archive.yml と gsc-archive.yml の取得stepに `set -o pipefail` を入れた。**同じ型の `| tee` が factory-check・fetch-gsc(2か所)・index-check にも残っている**が、これらは所見ありで非0を返す設計なので pipefail を入れると常時赤になる。**取得系（出力物が増えるべきもの）と監査系（非0が正常な結果）を混ぜて直さない。** 監視は「アーカイブの最終日 vs 今日」で行うのが正しい。
- [2026-09-16] **metrics.db の収集も 9/8 で止まっていた。原因はスクリプトではなくスケジュールタスクが `enabled: false` だったこと。** `ops-daily-metrics-collect` は 9/7 21:51 UTC を最後に発火せず、`collect.py` 自体は手動実行で正常に通った（1350件・noematch_ga4 420件）。**収集が止まったときは、まずコレクタではなくタスクの enabled を見る。** 現在 enabled のタスクは daily-job-patrol / noematch-daily-strategy / affiliate-noematch-seo-rank-watch の3本だけで、他は全て停止している。
- [2026-09-17] **LINE導線の設置を「その時点の着地ページの固定リスト」で3回やった結果、後から流入が来たページが漏れていた。** 8/27〜9/14のGA4アーカイブ（direct・(not set)除き）で3セッション以上の着地ページを `lin.ee` の有無と突き合わせると、8本に導線が無かった。最大は `/articles/hatsushon-nenmei-data/` の9セッション（bing 8）で、流入が始まったのは8/30——**AI着地向けの設置（8/29）の翌日**だった。8/31の bank_freshness（手書きリストで新設バンクが監視から漏れた）と同じ形。**設置対象は毎回アーカイブから計算し直す**（抽出条件は `scripts/add_line_cta_landing_0917.py` のdocstringに記載）。**根拠:** 9/17の突き合わせ。
- [2026-09-17] **導線の効果は `article` パラメータではなく内蔵 `click` の `pagePath × linkDomain=lin.ee` で測る。** 既存のLINE導線はすべて `line_add_click` に `article` を載せているが、9/4に確認したとおりカスタムディメンション未登録のパラメータはレポートに出ない。外部リンクなので内蔵の outbound click が別に発火し、ページ別に切れる。**根拠:** 9/4のGetMetadata（customEvent 0件）と、aff_click を内蔵 click で代替した実績。
- [2026-09-17] **令和7年 人口動態統計（確定数）は9/17時点で未公表。** e-Statの公表予定は「2026年9月」で日付未定、`kakutei25/index.html` は404。前年（令和6年）は9/16公表だったので、前年日付で「出ているはず」と見込んで作業を始めない。**根拠:** e-Stat release-calendar（kikanCd=00450）と厚労省URLの実測。

- [2026-09-18] **CIのGA4アーカイブは「欠けが無い日だけ緑」になる。** 9/16に pipefail を入れた後も `GA4_KEY_JSON` は未登録のままで、9/14〜9/16 の success はローカル手動実行で欠損0日だったから認証に進まず通っていただけ。欠損が1日出た 9/18 朝の run は正しく赤で落ちた（FileNotFoundError）。**pipefail は効いているが、シークレット登録（CEO作業）までは毎日ローカル実行で埋めるしかない。** 同日 `ops-daily-metrics-collect` も enabled:false のまま（最終 9/7）で、metrics.db は 9/16 の手動収集が最後だった。**根拠:** `gh run view 35286832389 --log-failed`・`gh secret list`（GSC_KEY_JSON のみ）・list_task_runs。
- [2026-09-19] **9/19朝のCI GA4アーカイブも同じ理由で失敗する見込みのまま（`GA4_KEY_JSON` は依然未登録・`gh secret list` は GSC_KEY_JSON のみ）。** 9/17分はローカルの `fetch_ga4.py` で補完し欠損0日に戻した。**シークレット登録（CEO作業）が済むまで、日次戦略タスクの冒頭でローカル補完を続ける。** 同日、アーカイブ9/10〜9/17を再計算するとLINE導線の無い着地ページは2セッション以上で2本だけ（nashikon-data・omiai-danjohi-data、いずれもbing主体のデータ記事）で、`scripts/add_line_cta_landing_0919.py` で埋めた。**3回の穴埋め（8/26・8/29・9/17）を経て、実流入のある着地ページの導線カバーはほぼ埋まった。以後この型の一手は残りが薄い。** **根拠:** `gh run view 35286832389 --log-failed`（GA4_KEY_JSON 空）・ga4_archive 9/10〜9/17 の by_page 集計。
- [2026-09-22] **`GA4_KEY_JSON` を登録してCIのGA4アーカイブを復旧した。鍵はローカルに実在しており、CEO作業として待つ必要は無かった。** 9/16・9/18・9/19 と3回「未登録・CEO作業待ち」と書いて毎日ローカル手動実行で補完していたが、`C:\Users\tatsu\matching-app\secrets\noe-ga4-key.json` は存在し、中身も ga4-archive.yml がコメントで指定する `gsc-ga4-analytics@matching-app-analytics.iam.gserviceaccount.com` と一致していた。`gh secret set GA4_KEY_JSON` にそのファイルを流し込む1コマンドで済み、手動 dispatch した run 35662704223 は 欠損2日（9/18・9/19）を取得して success。**原因を特定した回に、その場で直せるかを必ず確かめる。** 「CEO作業」と分類した時点で6日ぶんの手動補完と1日の欠損が発生した。**根拠:** `gh secret list`（登録前は GSC_KEY_JSON のみ→登録後2件）・run 35662704223 のログ「新規保存: 2日／保存日数: 65日（2026-07-17 〜 2026-09-19）」。
- [2026-09-22] **ローカル手動補完は、CIの故障を隠す。** 9/14〜9/16 の run が success だったのは 手動補完で欠損0日になっていて認証処理に進まなかったからで、9/16に書いた「pipefail は効いている」は正しいが、**補完を続ける限りCIは欠損が出た日にしか赤くならない**。回避策で計器を延命すると、故障の検知そのものが不定期になる。**根拠:** ga4_archive の fetched_at がローカル実行（9/16 10:03・9/18 21:38・9/19 07:17）で、CI実行は 9/21 22:27 が初。
- [2026-09-22] **`ops-daily-metrics-collect` は 9/7 を最後に enabled:false のまま（15日停止）。metrics.db は 9/18 の手動収集が最後。** 9/16・9/18 にも記録済みで、今回で3回目の同じ報告。現在 enabled のタスクは daily-job-patrol / noematch-daily-strategy / affiliate-noematch-seo-rank-watch / ebisu-bishoku-ig-post の4本だけで、残る約40本は一括で停止されている。**一括停止はトークン運用の判断と整合するので、noe-match 側の判断で再開しない。** 再開はCEO決裁。**根拠:** list_scheduled_tasks（enabled:false・lastRunAt 2026-09-07T21:51Z）・collect_log の最終行 2026-09-18T21:39。
- [2026-09-18] **結婚新生活支援バンクの初動（公開18日）: 器具は Google から拾われず、データ記事が拾われた。** ツール `/tools/kekkon-shinseikatsu-jichitai/` は GSC 表示0・GA4は direct 2（自社の可能性）。データ記事 `/articles/kekkon-shinseikatsu-data/` は表示30・「結婚新生活支援事業の対象 自治体 一覧」で平均6位、流入は Google 1・ChatGPT 1。予測（通園ナビ型で数日インデックス→Google/Yahoo経由）は**ツール側で外れた**。「いくら」「◯◯市 補助金」の語は表示0で、取れたのは「一覧」語だけ。婚活側クラスタでの器具の流入獲得は**まだ未達**。次はツールのインデックス有無（GSC URL検査）を確認すること。**根拠:** ga4_archive 8/31〜9/16・gsc_archive 8/31〜9/14 の by_query_page 集計。

## 当たるのは「形式」ではなく「語の型」だった（2026-09-19 実測・CEO決定）

GSC 8/18〜9/14。ツール26本＝表示951・クリック26、データ記事43本＝2,018・18、その他236本＝2,966・34。
ツール＋データ記事は本数23%でクリック56%。CEO決定（9/19）＝**新規生産はツールとデータ記事へ寄せ、
通常記事の新規はEクラスタ以外は止める**。

69本を1本ずつ「28日表示20以上かつ平均20位以内＝当たり」で判定した結果（`agent/cluster_analysis_20260919.md`）:
制度×自治体ナビ62%・制度×自治体データ60%・アプリ個社の公表値44%・公的統計43%に対し、
日付・手続き33%・俗語の診断20%・費用シミュレーター17%・民間調査の相場12%・事業者横断比較0%。

- **当たるのは、検索語が固有の名詞（制度名・自治体名・アプリ名・統計名）で、答えが一次資料の数字で決まる型。**
  外れるのは金銭語・比較語で、SERPに既に器具か大手の比較表がいる型。8/27の横断比較3組（6ページ）は合計で表示21。
  「自分たちが並べ直した」ことは検索需要にならない。
- **対（ツール＋データ記事）にしても当たり率は上がっていない**（ツール: 対あり18%・単独43%）。ただし対ありは
  横断比較型に偏っていて型の差と分離できない。対にする理由は当たり率ではなく、同じ正本で2経路を取ることに置く。
- **「公開後30日の表示」は基準として使えなかった。** 69本中41本が公開30日未満で、6〜7月公開分は
  インデックス遅延で初30日の表示が0。窓を固定した28日表示×平均順位で見るのが現実的。
- **需要があるのに受け皿が弱い語がGSCに出ている。** `tomobataraki-wariai-data` は表示515で平均54位、
  そこに「専業主婦 割合」「共働き世帯 割合 2022」のような別意図・年指定の語が着地していた。
  打順はここから取った（1位＝専業主婦の割合・tool_gate GO・月6,480）。
- **確認日つきの正本も、版が変わると黙って古くなる。** 8/27確認の共働き世帯の正本は白書 令和7年版（最新2024年）
  だったが、令和8年版（令和8年7月）が既に出ており、2025年は専業主婦世帯358万・共働き1,254万（3.50倍）。
  URLに版（r07）が入っている出典は、確認日にURLが生きていても最新とは限らない。**「最新版が出ていないか」を
  確認手順に入れる。** `bank_freshness.py` は確認日からの経過日数しか見ていない。

## セッション目標800は Direct 込みの土台に乗っていた（2026-09-19 改訂）

9/4 に置いた「現在 月560→9月末800→11月末1,500」は、GA4 の `total.sessions` をそのまま使っていた。
`agent/ga4_archive/` で取り直すと、8/13〜9/3（22日）は全体415（日18.9）のうち **Direct が285**で、
Direct除きは130（日5.9）しかなかった。Direct には自社の検品アクセスが混じる。
9/1〜9/16（16日）は全体250（日15.6）・Direct 76・**Direct除き174（日10.9・月換算 約330）**。
全体は減ったが、減ったのは Direct で、検索・AI経由はほぼ2倍になっていた。
ただし直近28日の前半14日→後半14日は Direct除きで141→148（ツール・記事ページの行合計では167→175）で横ばい。

- **セッションの計器は「total.sessions − by_page の channel=Direct の合計」に統一する。**
- by_page の行はページ×チャネル単位なので、複数ページを見たセッションは重複して数えられる
  （9/1〜9/16で by_page 合計286 対 total 250）。**by_page の行を足した値と total を同じ表に並べない。**
  上の定義は Direct 側だけが重複ぶん大きく出るので、Direct除きは控えめ（下限寄り）の値になる。
- 目標は月収と「結果直後のクリック数」を主、セッションを分母として併記する形へ改めた（`strategy_2026Q4.md`）。
  **ただし現行のアーカイブは内蔵 click の linkId／linkDomain 別件数を保存しておらず、この主KPIは後から
  再計算できない**（`fetch_ga4.py` の改修が要る＝自動化の変更なのでCEO承認事項）。

- [2026-09-20] **平均順位の改善は、表示回数を並べて見ないと読めない。** 頭語6語の9/20判定で、離婚率（19.5→6.8）・生涯未婚率（19.1→9.6）・結婚費用（8.8→4.0）の3本がページ平均順位を大きく上げたが、同じ窓で表示が 84→28 / 18→7 / 10→2 に落ちていた。平均順位は「表示されたクエリ」の加重平均なので、下位で細く拾っていた尾が消えるだけで数字は上がる。**順位の改善と表示の減少が同時に起きていたら、それは順位が上がった証拠にならない。** 逆に入籍日カレンダーは表示27→79・順位13.2→8.9で同方向に動いており、これだけが「動いた」と言える。**根拠:** GSC API・施策前12日（8/26-9/6）と施策後12日（9/7-9/18）のページ単位比較。
- [2026-09-20] **strategy_2026Q4.md の「着手前の順位表」（9/6）はページ単位の平均順位で、頭語クエリの順位ではなかった。** 頭語の完全一致で引き直すと 入籍日・結婚 費用・保育園 点数 は28日でも表示0で、表の 10.2 / 9.1 / 10.7 とは別の数字。「ガルガル期 ハブ20.5（頭語71.8）」だけが両方を併記していて、そこに気づく手がかりがあった。**順位を表に書くときは、ページ単位かクエリ単位かを必ず添える。** 添えないと2週間後の自分が別の指標と比べる。**根拠:** GSC APIで equals / contains / page の3通りを引き直して突き合わせ。
- [2026-09-20] **「ガルガル期」系のクエリで Google が上に出しているのは、9/8に最終回答型へ作り直したハブ記事ではなく診断ツールの方だった**（/tools/garugaru-check/ 表示35・8.1位 対 /articles/garugaru-ki-guide/ 表示37・18.2位）。頭語の受け皿をどのページにするかは、作り込みの量ではなく**Googleが実際に選んでいるページ**で決めるほうが早い。**根拠:** GSC 8/22-9/18・query contains「ガルガル期」× page。
- [2026-09-20] **判定日を「一度きりの自動タスク」に積む方式は、タスクが enabled:false のまま残ると誰にも気づかれずに落ちる。** 9/20 09:00 の頭語順位判定（`affiliate-headword-rank-verdict-20260920`）は fireAt が来ていたが enabled:false・lastRunAt 無しで、発火しない状態だった。9/29 の国勢調査更新（`affiliate-census-update-20260929`）も同じ状態。9/13予定のSERP確認も起動が遅れて9/16実行になっている。**判定予定は knowledge.md 末尾の「判定予定」節を正とし、自動タスクは補助と見なす**（日次戦略タスクが毎朝そこを読むので、そちらは落ちない）。**根拠:** `list_scheduled_tasks` の enabled / lastRunAt 実測。
- [2026-09-20] **metrics.db の収集タスク `ops-daily-metrics-collect` は 9/16 に原因を特定したあとも enabled:false のまま。** 現在 metrics.db に入っている最新は 2026-09-18 ぶんで、fetched_at は 2026-09-18 21:39 の手動実行。自動では積まれていない。一方 GitHub Actions 側の `agent/ga4_archive` `agent/gsc_archive` は生きている（GSC は 9/19 22:16 UTC の run が成功）。**noe-match の日次の計器はリポジトリのアーカイブ、metrics.db は手動時のみ更新される横断台帳、と用途を分けて読む。** 再有効化は全社のタスク一斉停止（enabled は4本のみ）と整合を取る必要があるのでCEO判断。**根拠:** metrics.db の fetched_at と `list_scheduled_tasks` 実測。

- [2026-09-21] **ツール9本の語寄せ（8/24・cycle_20260824）は判定不能。理由は交絡で、しかも計器が対象を含んでいなかった。** 9本の公開日は8/13〜8/23で、施策の前日（8/23・コミット4b4d12a）に sitemap-all の欠落3本の追加と、seikatsuhi / kekkon-shikin / app-kekkonritsu / koisaihi のインデックス申請が入っている。つまり pre 窓は「まだインデックスされていない期間」で、post との差は語寄せの効果とインデックス開始の効果が分離できない。実測（`agent/gsc_archive` 直読み・8/1〜8/23 対 8/25〜9/16 の各23日）は9本合計 imp 28→96・clk 0→1。公開前から存在した非対象ツール3本（garugaru-check 46→36 / konkatsu-type-shindan 16→23 / nyuseki-calendar 20→110）は合計 82→169・clk 7→8、サイト全体は imp 3029→4216。**施策の効果を凍結するのは、対象ページが公開から28日以上たってGSCに定常的な表示がある場合に限る。** 公開直後のページに施策を当てると、28日後に測れるのは施策ではなくインデックスの進行になる。**根拠:** gsc_archive 129日ぶん（5/11〜9/16・欠損0）の直読みと、8/23コミットの内容。
- [2026-09-21] **`scripts/pdca_cycle.py` は `/articles/` しか見ておらず、ツールを対象にした施策の凍結データは空だった。** cycle_20260824 は note に「ツール9本の語寄せ」と書いてあるのに、凍結された74ページは全部 articles で、ツールは1本も入っていない。判定日（9/21）に `check` を回すと、施策と無関係な記事の順位表が出てきて、読んだ人は判定できたと錯覚する。**計器が対象を含んでいるかは、凍結した時点で確認する。** `/tools/` を `tools/<slug>` というキーで拾うよう修正し（記事のslugと衝突させない）、freeze 時に「noteにツールと書いてあるのに凍結データにツールが0本」なら警告を出すようにした。あわせて、表に出していた順位が `best`（最良クエリの順位）で、ページの平均順位ではなかった点も是正し、表示加重の平均順位を併記した（9/20の知見「順位は表示と並べないと読めない」の機械化。表示が半減しているのに順位が改善している行には注記が付く）。**根拠:** `scripts/pdca_cycle.py` の `load_gsc()` 実装と cycle_20260824.json の中身。
- [2026-09-23] **11〜30位帯の押し上げ（8/26・cycle_20260826・4本のtitle/h2を実表示クエリへ寄せ＋アンカー128本）は効かなかった。狙った語は押し上がらず、4語中3語は表示ごと消えた。** `agent/gsc_archive` 直読みで 8/3〜8/25 対 8/27〜9/18（各23日・欠損0）。対象4本の表示は合計 190→54（−72%）、同期間のサイト全体は 3358→4388（+31%）。ページ別: shinkon-seikatsu-guide imp 21→8・平均順位 20.5→18.9／student-guide 74→1／kekkon-madeno-kikan-data 83→39・13.1→8.4・clk 0→2（4本で唯一のクリック）／propose-guide 12→6・33.3→41.5。狙った語: 「新婚生活 引越し」16.9→18.9位（imp 12→8）・「結婚 段取り」30.4→38.0位・「20代 社会人 マッチングアプリ」「マッチングアプリ 結婚までの期間 30代」「交際期間 結婚」は post で表示0。**11〜30位の語を title/h2 の語順だけで寄せても押し上がらない。** この帯の上限は元々+6クリック程度（8/26コミットの見積り）で、9/20の頭語判定（表示が1/3〜1/5に落ちた）と同じく、title差し替え直後は表示が減る方向に振れる。この型（語順寄せ）は11〜30位帯には追加投資しない。ただし母数は語あたり3〜16表示で小さく、アンカー128本の効果はページ単位の表示減に埋もれて分離できない。**根拠:** gsc_archive by_page / by_query_page の集計（今回のスクリプトはスクラッチで実行・再現は上記の窓で同じ集計）、コミット 9e10f7d。

- [2026-09-21] **GA4アーカイブの最終日は構造的に「今日−3日」までしか進まない。** `fetch_ga4.py` の取得窓は `date.today() - 2`（当日・前日は確定しないため）で、CIの発火が 22:4x UTC ＝ 翌日の 07:4x JST。したがって朝に見ると最終日は常にJSTの今日から3日前になる。9/21朝の時点で最終日 2026-09-17 は正常値で、収集停止ではない（9/19の run も「未取得 0日」で正しくスキップしている）。GSC は API 側の遅延が加わって今日−4〜5日。**日次戦略タスクの「3日以上離れていたら異常」というしきい値は、GA4は4日・GSCは6日で見る。** **根拠:** `scripts/fetch_ga4.py:123,297` と Daily GA4 Archive run 35474158552 のログ。

## 判定予定（結果が出たら知見に昇格させる）

- **10/6: 頭語6語のクリック込み判定（最重要・9/20判定を持ち越した分）。** 9/20はページ平均順位が上がった3本が同時に表示を1/3〜1/5に落としていて判定できなかった。10/6はクリック（表示と独立）で見る。既定ルール「4語のうち1語も1ページ目に入らなければタイトル型は打ち止め、本文の作り直しへ」。材料は `strategy_2026Q4.md`「9/20 頭語順位判定」節
- **9/29: 令和7年国勢調査 基本集計の公表確認と統計記事3本の更新。** 手順は `agent/census_update_20260929.md`。一度きりタスク `affiliate-census-update-20260929` は enabled:false で発火しないので、**この日の日次戦略タスクで実施する**（未公表なら触らない）

- 9/4: GSC日別アーカイブの自動実行（8/28新設）。`--stats` の「期間内の欠損」が0日のままかを見る。1日でも欠けていたらCIのcron発火を疑う

- 10/15: 着地ページ8本へのLINE導線追加（9/17）。GA4の `click × linkDomain=lin.ee × pagePath` で8ページ合計のクリックと、同期間の友だち数の増分を見る。同期間の8ページのdirect除きセッションを分母にする。0件でも母数が数十なので「効かない」とは判定しない
- 9/24: Bing流入の水準（9/14=12・9/15=11、それまで日1〜4）が続くか。9/14は13ページに1セッションずつ散っていて特定ページの順位上昇ではない。続けばBing側の評価変化、戻れば一過性
- 9/29: 人口動態統計 令和7年確定数の公表確認（婚姻率・離婚率）。9/23時点で未公表（`kakutei25/index.html` が404。前年の令和6年確定数は2025-09-16公表）。国勢調査の確認と同じ日に見る
- 9月中旬: 通園ナビの初動（GSC+Bing）。器具×一次データ×施行前の型の検証
- 8/27: 診断クラスタGA4判定（基盤は8/26整備済み）
- 9月中旬: 育休延長の条件判定（8/27公開）の初動。ゲートGO語2本目・法令系器具の初検証
- 9月中旬: 産後リカバリー診断のアンカー語寄せ（8/27実施・31本をブランド語先頭→「産後の抜け毛はいつまで？老けたと感じる時期の目安…」へ）。公開8/23で表示0からのスタートなので、初回表示が出るかどうかで判定する
- 9月中旬: 産後ケア料金ナビ（8/27公開・43自治体）の初動。tool_gate GO語の初検証＋「勝ちクラスタから内部リンクを引く」の効果。予測: 通園ナビ同様に公開数日でインデックス、Google/Yahoo経由で着地する
- 9月中旬: 育児休業給付金の記事（8/27公開）の初動。**追加取得ゼロで出した2本目の面**が、一次取得した面と同じくらい流入するかの検証。予測: データ記事の平均（1.28セッション/本）前後。AI・Bing経由で着地する
- 9月中旬: 産後ケアの日帰り型・訪問型（8/29公開）の初動。**1つのバンクから6面まで出したときに、後から出した面も同じだけ拾えるか**の検証。産後ケアバンクは3類型＋回数＋助成＋申し込みで6面になった。予測: 先行4本と同水準（データ記事の平均1.28セッション/本前後）。ここで落ちるなら「1バンクあたりの面の数には上限がある」ことになり、新しいバンクを取る判断に変わる
- 9月下旬〜2027年3月: 出産費用の医療費控除（8/31公開）の初動。**法令ルール×実額バンクの掛け合わせ**という面の作り方の検証。予測: データ記事の平均（1.28セッション/本）前後、AI・Bing経由。ただし確定申告期（2〜3月）に需要が立ち上がる季節語なので、**9月時点で0でも失敗と判定しない**。9月下旬にインデックスの有無だけ見て、流入の判定は2027年3月に行う


## `.table-scroll` にCSSが無かった（2026-08-27 発見・修正済み）
表を包む `<div class="table-scroll">` は24ページで使われていたが、**このクラスのCSSがどこにも
定義されていなかった**。結果、モバイル（375px）で表が横にはみ出し、ページ全体が横スクロールしていた。
`.table-scroll{overflow-x:auto}` と `article a{overflow-wrap:anywhere}` を
`tools/hoikuen-tensu-nerima/index.html` の style（全ツール・全記事のCSS供給元）に追加し、
既存の出力にも直接あてた。修正後は375pxで横はみ出し0。
**検証は必ずビューポートを与えてから行う**こと。バックグラウンドタブは `clientWidth` が0を返し、
すべての要素が「はみ出している」と誤検出される。

## 出典の説明文が「公的統計」で固定されていた（2026-08-27 発見・修正済み）
`_article_shell.source_list()` が出典の前に「本記事の数値はすべて次の公的統計の公表値です」を
必ず書いていた。自治体の公式ページや企業のIR資料を出典にした記事では、ここだけ事実と食い違う。
`SRC_INTRO_TOUKEI` / `SRC_INTRO_JICHITAI` / `SRC_INTRO_KIGYO` の3種を用意し、
記事ごとに渡すようにした。**共通シェルの固定文は、出典の性格が変わると嘘になる。**


## GSCのサイトマップ登録は、URLを間違えると恒久的なエラーとして残る（2026-08-27 解消）
2026-07-26に `/sitemap.xm`（末尾の l が欠落）を送信してしまい、以後ずっと取得失敗の
エラー1件がサイトマップレポートに出続けていた。サイト側にそのファイルは無いので
自然に消えることはなく、**GSC側の登録を削除するしか消す方法がない**。
8/27にCEO承認を得てAPIで削除（DELETE→204）。エラー合計は0になった。

判断に使える実測メモ:
- サイトマップAPIの `indexed` は**常に0を返す**。インデックス数の判断には使えない。
  使うのは URL Inspection API の `coverageState`。
- `lastDownloaded` が None なら、そのサイトマップは一度も取得できていない。
  綴り間違いの登録かどうかは、この値で機械的に判定できる。
- GoogleのIndexing APIは JobPosting と BroadcastEvent 専用。通常ページには使えない。

## tool_result は2026-08-29以前の値を信用しない（計測バグ修正の断絶）

バンク由来ツール等は初期表示のrender内で `tool_result` を送っていたため、
**閲覧しただけで「結果表示」が計上されていた**（実測: 7日間で tool_result 156 に対し
tool_calc 10）。2026-08-29 に `window.__ui`（pointerdown/keydown/change で立つフラグ）で
ガードし、ユーザー操作後のみ発火に修正した（23ツール・未ガード残0）。

**8/29以前の tool_result は使用実態より大きい。前後比較をしてはいけない。**
9月中旬のバンク初動判定では 8/30以降の値だけを使うこと。
tool_calc・診断系の show/calc（ボタン押下起点）は元から正しく、影響なし。

- [2026-08-31] **レポートの期間表示を実運用期間と混同しない。** A8のサイト別レポートが「2026年1〜8月」を返したので8で割り「月12クリック」と結論したが、**運用開始は6月**で1〜5月は存在しない。正しくは月32件（8月は概算48件）。月12件なら判定不能、月48件なら3〜6か月で判定可能——結論が真逆になる。**分母は必ずサイトの開始月と突き合わせる。** **根拠:** CEO指摘＋A8サイト別レポート実測。
- [2026-08-31] **3か月目のサイトを静止値で外挿してはいけない。** 「月48クリック×転換0だから¥300,000には100〜375倍要る＝延長線上では届かない」と結論したが、実測の傾きは**全期間110日の累計クリック69のうち46%が直近14日**（14日窓で32←18）＝複利が効き始めた形。静止値からの必要倍率計算は成長期のサイトには使えない。**根拠:** `scripts/cluster_gsc.py` と gsc_archive 実測。
- [2026-08-31] **A8の成果は「サイト別レポート」で見る。** 簡易レポートと旧「全案件」はアカウント全体（当社は5サイト登録）。ただし今回は**noe-matchが91%を占めていた**ので、混入を疑った推測の方が外れた。**推測で切り分ける前にサイト別を出す。** 経路は新管理画面 `media-console.a8.net/report/site`。**旧管理画面のレポートURL直打ちはセッションエラーになる**（提供終了予定）。**根拠:** 8/31実機。
- [2026-08-31] **発生0件は、クリック数が少ないうちは何の証明にもならない。** 真の転換率が1%でも96クリックで0件になる確率は約4割。**0%と1〜2%の判別には250〜300クリック要る。** 「成果0＝オファーが悪い」と早期に結論すると、答えが出る直前に実験を止めることになる。**根拠:** 二項分布（0.99^96≈0.38）＋A8実測96クリック。
- [2026-08-31] **自社の検品・QA作業がGA4を汚す。** 8/30の `tool_result` 10件は、検品エージェントがひとり親ツールで6区を切り替えた自社操作の可能性が高い（同日のセッションは14）。**エージェントに実機確認をさせた日は、イベント数から差し引いて読む。** 8/26に内部トラフィック除外を有効化済みだが、エージェントのブラウザは除外対象外。**根拠:** 8/30のイベントと検品ログの突き合わせ。
- [2026-08-31] **公開を伴う成果物は、公開する前に検品を通す。** ひとり親バンクを公開後に検品してBLOCK（正本データの内部作業メモが読者に露出・「23区で唯一」という自ページと矛盾する全称）。露出済みからの同日是正になった。**エージェントに公開まで任せる場合は「push前に検品を差し込む」か「下書き止めで返す」を指示に含める。** 再発防止として `scripts/build_hitorioya.py` に禁止語リントを実装（初回ビルドで清書漏れ1件を実際に検出）。**根拠:** 8/30-31の事故と是正。
- [2026-08-31] **実査データの note 欄は「読者向け」と「内部QA」を分ける。** 検算の経緯・取得手段・基準訂正の記録を同じ欄に書くと、生成物にそのまま出る。**読者向け=note／内部=qa_note** に分け、ビルド側で qa_note を除去＋禁止語リントで二重に止める。**根拠:** 同上。
- [2026-08-31] **空白地帯は「儲からないから空白」。** 勝てているのは制度・産後・運用（週13クリック）だが、これらは弁護士も大手も収益化できないから空いている。一方で収益経路のある婚活側は週8クリック。**勝てる場所と金が出る場所がずれているのが構造。** 集客クラスタの出口（LINE）を開けない限り、6割のクリックは0円のまま。**根拠:** `cluster_gsc.py` のクラスタ別実測＋アフィリ設置の棚卸し。
- [2026-08-31] **アフィリの設置漏れは収益0の原因ではない。** 195/289ページに設置済みで、表示上位20ページは全部オファー付き。問題は**置き場所が「表示」基準で「クリック」基準になっていない**こと（tokyo-guide は30日で表示233・クリック0なのにアフィリ5本／誰でも通園ナビは CTR12.5% なのにOisix1本）。**根拠:** 全ページのhref棚卸し×GSC30日実績の突き合わせ。
- [2026-08-31] **ゲート検証は着工前に必ず回す（2本とも判断が変わった）。** 学童保育料バンクは本命視していたが**NO-GO**——住まいサーフィンの23区比較表が2クエリで1位、学童のくちコミが一都三県770施設のDBを週3本更新で運用中。「横断表が無い」という読みが誤りだった。結婚新生活支援は**条件付きGO**——ゼクシィすら一次確認を放棄しているが、23区は原則未実施なので**スコープは一都三県**。**根拠:** 8/31のSERP・サジェスト・note供給の実測。

- 9月末: noteの流入判定。note運用は8/29開始（8/29〜9/27に60枠中40本予約）で、**8/30までの28日窓ではnote稼働が2日しかなく、note.com経由は3セッションしかない**。ここで「noteは効かない」と結論してはいけない。9/28以降にGA4のsource=note.comで測る

- [2026-09-06] **生成スクリプトの再実行は、公開後に当てたパッチを消す。** `build_nyuseki_tool.py` と `build_toukei_articles.py` を title 変更のために再実行したら、8/29の `window.__ui` ガード（tool_result の計測修正）、結婚資金計算機の基準額更新、add_* で追加した関連リンク3本が生成物から消えた。生成物は build 後に別スクリプトで何度も上書きされていて、**build スクリプトは生成物より古い**。既存ページの文言を直すときは**生成物のHTMLを直接直し、build スクリプトには同じ文言を写すだけ**にする（再実行しない）。再実行が要るときは `git diff --stat` で title 以外の差分が出ていないかを必ず見る。**根拠:** 9/6の実測（git checkout で復旧済み・未公開）。
- [2026-09-06] **title を差し替えるときは H1 も同時に揃える。** 8/29の差し替え8本のうち with 2本は、8日後の実SERPで旧タイトルが出ていた。クロール遅れではなく、**Googleが title を捨てて H1（旧文言のまま）を表示していた**。title だけ変えると、SERPには何も起きないまま「効かなかった」と誤判定する。反映確認は GSC ではなく実SERP（`tool_gate.serp_titles`）で行う。**根拠:** 9/6の実SERP確認と H1 の突き合わせ。

- [2026-09-19] **高単価の保険相談を、流入のある制度ツール側へ置いた（広告の置き場所と流入のズレの是正）。** FPカフェ11,700円・保険ランドリー11,329円の受け皿は流入がほぼ0の `kekkon-hoken-minaoshi` だけだった。新規提携はせず既存の保険ランドリータグ（`4B8B4Q+42GRGA`）を流用し、制度ツール5本（kodomo-iryohi-jichitai / daredemo-tsuen-jichitai / funin-josei-jichitai / kosodate-shien-23ku / hitorioya-shien-jichitai）の `#result` 内・LINE CTA直後に `id="aff-hoken"` で設置。shussan-hiyou-data は「数字を読むときの注意」の直後に設置した（いずれもYMYL扱いで断定せず判断材料の提示に留め、PR表記つき・1ページ1枠）。あわせてDクラスタ・1ページ目の婚活ページ3本を決断地点へ寄せた（with-guide＝「合わせて検討したいアプリ」節の直後に追加 `aff-decide`／pairs-marriage-data＝比較表の節の直後に追加 `aff-compare` ＋ 記事外にあった末尾ブロックを本文のまとめ直後へ移設 `aff-conclusion`／with-seriousness-data＝FAQ後にあったブロックを結論節の直後へ移設 `aff-conclusion`）。頭語押し上げロック中の nyuseki-calendar・kekkon-okane-data は既存広告に `id="aff-shikijo"` を付けるだけで可視変更なし。Aクラスタは当初は触らなかったが、同日のCEO裁定（クラスタAにも収益導線を置いてよい・AGENT.md の保険ランドリー行が正）を受けて、`tools/sangokea-ryokin` の `#result` 内・LINE CTA直後に保険を新設（`aff-hoken`）、`tools/garugaru-check` は結果内に既にあった保険アンカーへ `id="aff-hoken"` を付けてカードの文面を「家計の見直し」主語へ差し替えた（YMYL枠は1ページ1箇所のため2本目は置いていない）。**`aff-hoken` の対象は計8ページ**（制度ツール5本＋shussan-hiyou-data＋sangokea-ryokin＋garugaru-check）。**判定は10/20に GA4 内蔵 click × linkId × pagePath で行う**（`aff-hoken` / `aff-decide` / `aff-compare` / `aff-conclusion` / `aff-shikijo`。ツール側の分母は「結果を出した人」＝`#result` 表示後に限られる）。変更前後の位置は `agent/seo/cta_baseline_20260919.json`、対象11ページは improvement-log の locks に 10/20 まで登録済み。**根拠:** 9/18〜19のGA4・GSC実測（Dクラスタ28日66セッションに対し広告クリック1件／1ページ目90本）と `agent/affiliate_targets.md` 198-199行の単価。
- [2026-09-19] **SERPの見切れは一部のページではなく全体構造だった。** articles/tools の実運用257ページ（移動済みリダイレクト48件を除く）のうち216本（84.0%）でtitleが32字超、GSC28日窓（8/18〜9/14）の表示合計に占める32字超ページの割合は87.4%——つまり表示のほとんどがそもそも見切れているtitleから来ている。article/toolページのtitleにはサイト名サフィックスが付いておらず、本体全部がSERPの見切れ対象になる点も確認済み（サフィックスがあるのはトップページのみ）。表示20回以上かつ32字超の42ページを対象にtitleを32字以内へ短縮（`agent/seo/title_tighten_20260919.json`）。**H1は今回は変えていない**ため、9/6の知見（titleだけ変えるとGoogleがH1を表示し続けて効果が出ない場合がある）が再発するリスクがある。7日判定（〜9/27）でCTRが動かない場合は、まずtitleがSERPに反映されているか実SERPを見てから「効かなかった」と結論すること。**根拠:** `agent/gsc_archive/2026-08-18〜09-14` 実測＋inspector検品PASS（BLOCK1件→修正）。
- [2026-09-19] **小さいクラスタは、ロック中ページを除くと内部リンクの新規候補が0になることがある。** `scripts/cluster_gsc.py` の「統計」クラスタ（TOUKEI）は暫定3本（rikonritsu-data／shougai-mikonritsu-data／tomobataraki-wariai-data）しかなく、ターゲットのshougai-mikonritsu-data以外の2本が両方ともlocks[]でロック中（頭語押し上げ10/6・国勢調査10/13）だったため、同一クラスタからの新規内部リンクを1本も追加できなかった。「生涯未婚率」を本文で言及する他クラスタの15ページも確認したが、全ページが既にリンク済みで空きが無かった。**クラスタが小さい（3〜5本）ターゲットに内部リンク施策をかける前に、まずクラスタ内のロック状況を確認する。** ロック解除待ちで先送りにするか、CEO判断でクラスタ外からのリンクを許可するかを先に決めておくと手戻りがない。また、広告位置ロック（10/20判定）でtitle変更が解禁されたページ（with-guide・pairs-marriage-data・with-seriousness-data）のうち、with-seriousness-dataは事前のtitle提案（scratchpad `title_proposals.md`）に個別案が存在しなかった（同ファイルは別セッションの通常GSC改善調査で作成されたもので、広告ロック解除の対象範囲とは一致していなかった）。指示で名指しされたページに個別提案が無いケースがあるため、**「案1を使う」という指示を受けたら、まず該当ページの提案が実在するかを先に確認する。** 今回は先頭句維持＋副題削除の標準ルールで代替作成し、報告で明記した。**根拠:** `agent/seo/internal_links_20260919.json`・`agent/seo/title_tighten_20260919.json` 作成時の実測。
- [2026-09-23] **9/19に張った判定ロックが、9/23の打ち手をほぼ全部止めていた。** 「0クリックだが順位は取れている面の回収」を最新28日（8/23〜9/19）で選び直すと、表示20以上・平均20位以内・クリック0は**12ページ（表示596）**。ところがこの12のうち**11がロック中**で、触れるのは `garugaru-doukyo` の1本だけだった。ロックの内訳は site-wide title tighten（10/05）・内部リンク強化（10/08）・広告位置（10/20）・頭語押し上げ（10/06）で、いずれも9/19に自分で張ったもの。**回収したい面と、9/19に施策を当てた面は同じ集合**なので当然こうなる。横展開側も同じで、打順#4（高校生の医療費）は出荷基準4「同クラスタから内部リンク3本以上（ロック中には張らない）」を満たせない——`kodomo-iryohi-data`（10/08）・`kodomo-iryohi-jichitai`（10/20）・`kosodate-shien-23ku`（10/20）という話題の近い3本が全部ロック中で、SEIDOクラスタで空いている9本は出産費用・育休・ひとり親と話題が遠い。**施策を打つ前に、その面が自分のロックで埋まっていないかを先に見る。** 打順は需要だけで並べてあってロックを見ていないため、打順の順番どおりには着手できない。**根拠:** `agent/gsc_archive/` 直近28日の by_page 集計と `agent/seo/improvement-log.json` の locks[] 76件の突き合わせ。
- [2026-09-23] **`_article_shell.py` は `id="aff-..."` を出力していなかった。** 9/23に広告リンク338本へ計測idを付けた作業は**生成済みHTMLだけ**に当てたもので、生成側のシェルは素のままだった。つまり **データ記事をビルドし直すと id が黙って消え、10/20の広告位置判定からそのページが抜ける**。`write()` に `aff_id` を足し、既定を `aff-oisix`、`aff-` で始まらなければ ValueError で落とすようにした。**生成物に後から当てた修正は、生成側にも同じ修正を入れたか必ず確かめる**（9/23の媒体ID差し替えでは生成側8ファイルも直していたのに、id付与のほうは漏れていた）。**根拠:** `grep -n 'aff-' scripts/_article_shell.py` が0件／ビルド済みの `articles/kodomo-iryohi-data/index.html` には `id="aff-oisix"` が実在。

