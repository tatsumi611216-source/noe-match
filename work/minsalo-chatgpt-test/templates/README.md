# templates/

`CLAUDE.md` は、**実際に台本を書く作業ディレクトリにコピーして使う**。

```bash
mkdir -p ~/work/chatgpt-daigaku && cd ~/work/chatgpt-daigaku
cp <このリポジトリ>/work/minsalo-chatgpt-test/templates/CLAUDE.md .
cp -r <このリポジトリ>/work/minsalo-chatgpt-test/tools .
```

録画の冒頭で `cat CLAUDE.md` を1回打つ。読み上げは不要。
